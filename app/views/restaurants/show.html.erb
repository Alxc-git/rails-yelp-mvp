<div class="container mt-5">
  <h1><%= @restaurant.name %></h1>

  <p>Address: <%= @restaurant.address %></p>
  <p>Category: <%= @restaurant.category %></p>

  <div class="mt-5 mb-5">
    <%= link_to "Back to the menu",  restaurants_path, class: "btn btn-outline-primary"  %>
    <%= link_to "Add a review", new_restaurant_review_path(@restaurant.id),class: "btn btn-outline-primary" %>
  </div>

  <h2>Reviews</h2>

  <% @reviews.each do |review| %>
  <ul>
    <li>
      <%= review.content %>
      <%= '⭐' * review.rating %>
    </li>
  </ul>
  <% end %>
</div>
